# Classic (session-based) CSRF protection for forms and logins/logouts.
# This project has no JavaScript build pipeline (minimal front-end), so the
# newer stateless/JS-based CSRF tokens are not used here.
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids: []
